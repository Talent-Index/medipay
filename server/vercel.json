{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/src"
        }
    ],
    "installCommand": "npm install && npm run postinstall",
    "outputDirectory": "dist"
}